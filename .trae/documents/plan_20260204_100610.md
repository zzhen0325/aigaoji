I will optimize the UI to match the Apple Stocks app's visual style, focusing on Dark Mode, large typography, and specific color palettes.

### 1. Configuration & Global Styles
- **Tailwind Config**: Add Apple-specific colors (System Gray, System Red/Green, System Blue) and enable `darkMode: 'class'`.
- **Global CSS**: Set default background to Apple's dark theme preference and refine font rendering for better legibility.

### 2. Component Architecture Refactoring
- **Theme Integration**: Ensure `useTheme` is correctly initialized in the application root to support instant dark/light mode switching.
- **Layout Component**:
  - Implement a translucent, blurred navigation bar (`backdrop-blur-xl`) similar to iOS navigation bars.
  - Update background colors to `system-background` (Dark: `#000000`, Light: `#F2F2F7`).

### 3. Page Redesigns
- **Home Page**:
  - **Header**: Implement "Large Title" style typography.
  - **Search**: Redesign as a rounded, gray-background input field.
  - **Lists**: Convert "Recommended Funds" into an "Apple Stocks" list style (Black background, clean separators, sparkline placeholder, price button).
- **Fund Detail Page**:
  - **Header**: Emphasize the real-time valuation with massive typography.
  - **Chart**: Switch to a minimalist line chart with gradient fill and no grid lines.
  - **Holdings Table**: Clean up the table to remove borders and increase spacing, matching iOS `List` views.
- **Portfolio Page**:
  - Redesign as a "Dashboard" with dark cards and high-contrast financial data.

### 4. Color System (Chinese Market Adaptation)
- **Red (Up)**: Use Apple's vivid red (`#FF453A` in Dark Mode).
- **Green (Down)**: Use Apple's vivid green (`#30D158` in Dark Mode).
- **Text**: High-contrast White for primary text in dark mode, System Gray for secondary text.
