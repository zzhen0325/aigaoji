I will optimize the UI to match Google AI Studio's Material Design 3 style.

### 1. Visual Design System
- **Color Palette**: Switch from Apple's system colors to Google's "Gemini" palette.
  - **Light**: White (`#FFFFFF`) background, Light Blue-Grey (`#F0F4F9`) surfaces, Google Blue (`#0B57D0`) accents.
  - **Dark**: Deep Grey (`#131314`) background, Surface Grey (`#1E1F20`), Pale Blue (`#A8C7FA`) accents.
- **Typography**: Use standard sans-serif with Material Design spacing (Roboto-like).
- **Shapes**: High border radius (pills/rounded rectangles) for inputs and buttons.

### 2. Configuration Changes
- **Tailwind Config**: Define `google-*` color tokens replacing `ios-*`.
- **Global CSS**: Update body background and text defaults.

### 3. Component Redesign
- **Layout**: 
  - Replace the translucent iOS navbar with a solid or minimal Material top bar.
  - Use "Pill" shaped active states for navigation links.
- **Home Page**:
  - Transform the search bar into a "Prompt Input" style (large, rounded, light grey background).
  - Style the fund list as Material Cards with tonal backgrounds.
- **Fund Detail Page**:
  - Use "Container" styles for data blocks (filled cards with no borders, just tonal difference).
  - Update charts to use Google brand colors.
- **Portfolio Page**:
  - Implement a card-based grid layout ("Masonry" or standard grid) typical of Google dashboards.

### 4. Interactive Elements
- **Buttons**: Fully rounded (`rounded-full`) filled buttons.
- **Inputs**: Filled style with underline or full rounded corners, no outer borders.
